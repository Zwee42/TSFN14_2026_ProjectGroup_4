apiVersion: apps/v1
kind: Deployment
metadata:
  name: backend-deployment
spec:
  replicas: 2
  selector:
    matchLabels:
      app: backend
  template:
    metadata:
      labels:
        app: backend
    spec:
      containers:
        - name: backend
          image: task3acr2fft0n.azurecr.io/backend:latest
          ports:
            - containerPort: 3000
          imagePullPolicy: Always
