# MongoDB Atlas Connection String
# Get your connection string from MongoDB Atlas: https://cloud.mongodb.com
# Replace <username>, <password>, and <cluster> with your actual values
# Note: w=majority ensures data is written to majority of replica set members
# This provides data durability but may impact write performance for high-throughput apps
MONGODB_URI=mongodb+srv://<username>:<password>@<cluster>.mongodb.net/taskmanager?retryWrites=true&w=majority

# Next.js Configuration
NODE_ENV=production

# Optional: Custom Port (default is 3000)
# PORT=3000
